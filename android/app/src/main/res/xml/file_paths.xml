<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Exposes filesDir/update.apk (written by main.py's Android update flow) to the system
         installer - path-name is arbitrary, just needs to match what FileProvider.getUriForFile
         resolves against in MainActivity.kt. -->
    <files-path name="updates" path="." />
</paths>
