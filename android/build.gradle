// Root build file. Version numbers below are a reasonable, current-as-of-training-data guess,
// NOT verified against a real Gradle sync - see android/README.md for what to check first if
// this fails to sync.
plugins {
    id "com.android.application" version "8.2.2" apply false
    id "org.jetbrains.kotlin.android" version "1.9.22" apply false
    id "com.chaquo.python" version "15.0.1" apply false
}
