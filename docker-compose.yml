services:
  bot:
    build: ./bot
    restart: unless-stopped
    volumes:
      - ./data:/app/data

  web:
    build: ./web
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
